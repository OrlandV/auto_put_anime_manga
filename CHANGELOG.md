### Версия 0.6.17 20260419
* _Поиск продолжений anime в **World Art** и формирование словаря страниц_ (`world_art.anime_pages`) теперь
  обрабатывает (пропускает) пустой раздел `Информация о серии`.
* _Нормализация наименования_ (`decode_name.normal_name`) теперь заменяет символ `ä` на `a`.
* _Первичный поиск XML-страниц манги и anime в **ANN**_ (`animenewsnetwork.search_pages`) теперь обрабатывает строку
  с количеством результатов, когда стока начинается с числа, но сразу за числом без пробела стоит не цифровой символ,
  например `+`.
* _Получение страницы (HTML-кода) из **Wikipedia** (en)_ (`wikipedia_.Page.__init__`) теперь обрабатывает отрицательный
  результат поиска.

### Версия 0.6.16 20260416
* _Извлечение даты премьеры anime из XML-страницы в **ANN**_ (`animenewsnetwork.anime_date_of_premiere`) теперь,
  не найдя тег с аттрибутом `gid`, берёт первый тег.
* _Проверка наличия наименований **ANN** и **Wikipedia** (en) в **World Art**_ теперь учитывает возможное отсутствие
  страниц (данных) в соответствующих ресурсах.

### Версия 0.6.15 20260414
* _Извлечение даты премьеры anime из XML-страницы в **ANN**_ (`animenewsnetwork.anime_date_of_premiere`) теперь
  пропускает теги, не содержащие аттрибут `gid`. Это позволяет пропускать сомнительные даты.
* _Извлечение из страницы частей страницы (частей инфо-блоков) по отдельным манге и anime в **Wikipedia** (en)_
  (`wikipedia_.manga_anime_in_page`) теперь в возвращаемом словаре по пути `[page_title]['manga' | 'anime'][title]`
  вместо части страницы (`BeautifulSoup`) находится словарь `{'page_part': page_part: BeautifulSoup, 'titles': {'eng':
  title_eng: str, 'orig': title_orig: str, 'rom': title_rom: str}}`. Т.е. вместе с частью страницы возвращается словарь
  наименований: английского, оригинального и ромадзи. Соответственно изменены параметры и/или результат следующих
  функций:
  * _фильтр частей страниц, удаляющий повторы, и переформатирование словаря частей страниц_
    (`wikipedia_.filter_page_parts`);
  * _извлечение данных из частей инфо-блоков в **Wikipedia** (en)_ (`wikipedia_.extraction_data`);
  * _извлечение данных по манге из соответствующей части инфо-блока в **Wikipedia** (en)_
    (`wikipedia_.extraction_manga`);
  * _извлечение данных по anime из соответствующей части инфо-блока в **Wikipedia** (en)_
    (`wikipedia_.extraction_anime`);
  * _извлечение оригинального наименования манги или anime из соответствующей части инфо-блока в **Wikipedia** (en)_
    (`wikipedia_.title_orig`).
* _Извлечение данных по манге из соответствующей части инфо-блока в **Wikipedia** (en)_ (`wikipedia_.extraction_manga`)
  в возвращаемом словаре теперь имеет ромадзи наименование манги.
* _Извлечение данных по anime из соответствующей части инфо-блока в **Wikipedia** (en)_ (`wikipedia_.extraction_anime`)
  в возвращаемом словаре теперь имеет ромадзи наименование anime.

### Версия 0.6.14 20260402
+ В модуле _конфигурации_ (`config`) к _жанрам AnimeNewsNetwork_ добавлены
  `{'mystery': 'мистика', 'thriller': 'триллер'}`.
+ В модуле _поиска страниц в **AnimeNewsNetwork** и их обработки_ (`animenewsnetwork`) добавлена функция _коррекции
  наименований в словаре данных манги или anime в **ANN**_ (`animenewsnetwork.fix_name`), которая используется
  в функциях _извлечения данных манги из XML-страницы в **ANN**_ (`animenewsnetwork.extraction_manga`) и _извлечения
  данных anime из XML-страницы в **ANN**_ (`animenewsnetwork.extraction_anime`).

* _Поиск продолжений anime в **World Art** и формирование словаря страниц_ (`world_art.anime_pages`) теперь:
  * отфильтровывает _отменённые проекты_;
  * принимает параметры:
    * _словарь уже найденных страниц anime_ `pages`,
    * _множество, список или кортеж ID уже найденных страниц anime_ `aids`.
* _Извлечение ID anime в **World Art** из страницы манги в **World Art** и формирование списка_
  (`world_art.anime_id_from_manga`) теперь отфильтровывает _отменённые проекты_.
* _Первичный поиск XML-страниц манги и anime в **ANN**, передача результатов в функцию pages и возврат её результатов_
  (`animenewsnetwork.search_pages`) теперь:
  * не запрашивает XML-страницу anime, если в тексте ссылки указано `live-action` или `novel`, или `stalled`;
  * не запрашивает XML-страницу манги, если в тексте ссылки перед искомым наименованием имеется символ.
* _Извлечение изданий из страницы в **World Art**_ (`world_art.publication`) теперь найдя издание `Asuka` (издательства
  `Kadokawa Shoten`), переименовывает его в `Gekkan Asuka`.
* _Извлечение даты премьеры манги из XML-страницы в **ANN**_ (`animenewsnetwork.manga_date_of_premiere`) переработана
  и теперь корректно обрабатывает строку даты формата `yyyy to yyyy`.
* В _поиске и получении XML-страниц манги и anime в **ANN** на базе уже полученных XML-страниц_
  (`animenewsnetwork.pages`) изменена логика
  * _обработки XML-страницы anime, получения и добавления в соответствующие словари связанных XML-страниц_
    (`animenewsnetwork.pages.apages`) и
  * _обработки XML-страницы манги, получения и добавления в соответствующие словари связанных XML-страниц_
    (`animenewsnetwork.pages.mpages`)

  с целью корректного разделения anime и манги по соответствующим словарям.
* В _поиске страниц по наименованию в **Wikipedia** (en)_ (`wikipedia_.search_pages`) исправлен поиск блока
  `infobox-full-data` и добавлен подзаголовок `Related media`.
* _Извлечение из страницы частей страницы (частей инфо-блоков) по отдельным манге и anime в **Wikipedia** (en)_
  (`wikipedia_.manga_anime_in_page`) теперь
  * исправлен фильтр блока `infobox-full-data`;
  * отфильтровывает блок с подзаголовком `Live-action film`;
  * сохраняет результат выборки, если последней обрабатываемой строкой была полезная.
* _Извлечение авторов манги или режиссёров anime из XML-страницы в **ANN**_ (`animenewsnetwork.authors`) теперь
  корректно обрабатывает имена только из одного слова.
* _Извлечение издания манги из XML-страницы в **ANN**_ (`animenewsnetwork.publication`) теперь:
  * обрабатывает все теги `related-prev` с атрибутом `rel='serialized in'`;
  * если найдены альтернативные наименования издания, то запрашивает у пользователя выбор подходящего наименования.
* _Извлечение оригинального и ромадзи имён из HTML-страницы автора в **Wikipedia** (en) и добавление в список result_
  (`wikipedia_.authors.orig_rom`) теперь:
  * не найдя тега `th` класса `infobox-above`, ищет тег `caption` класса `infobox-title`;
  * уточнён тег `span` в поиске оригинального имени;
  * если оригинальное имя не найдено, копируется ромадзи;
  * если HTML-страница не получена, то берётся не всё наименование страницы (адрес), а до `_(`.
* _Извлечение количества томов манги из соответствующей части инфо-блока в **Wikipedia** (en)_
  (`wikipedia_.number_of_volumes`) теперь учитывает возможное наличие сноски сразу после числа.
* _Извлечение количества глав манги из соответствующей части инфо-блока в **Wikipedia** (en)_
  (`wikipedia_.number_of_chapters`) теперь:
  * перебирает ссылки (теги `a`) в поиске `List` в адресе, тем самым фильтрует сноски;
  * в коде страницы списка томов ищет заголовок `h2` с `id="Volumes_list"`.
* _Извлечение формата anime из соответствующей части инфо-блока в **Wikipedia** (en)_ (`wikipedia_.anime_format`)
  теперь, не найдя формат, ищет `movie` в ссылке в ячейке `infobox-image`.
* _Извлечение даты премьеры из части страницы в **Wikipedia** (en)_ (`wikipedia_.date_of_premiere`) теперь обрабатывает
  строку `Release dates`.
* _Извлечение студий anime из соответствующей части инфо-блока в **Wikipedia** (en)_ (`wikipedia_.studios`) теперь
  обрабатывает строку `Production company`, если нет строки `Studio`.

### Версия 0.6.13 20260324
- В модуле _конфигурации_ (`config`) удалены:
  - словарь _куки **Wikipedia** (en)_ (`COOKIES_WP`),
  - _версия операционной системы Windows_ (`WINDOWS_VER`),
  - _версия Firefox для User-Agent_ (`FIREFOX_VER`).
- В модуле _константы_ (`constants`) удалена `USER_AGENT`.

+ В модуле _поиска страниц в **World Art** и их обработки_ добавлена функция _извлечения заголовка манги
  в **Wikipedia** (en) по ссылке в **World Art**_ (`world_art.wp_manga_title`).

* _Добавление манги в словарь страниц манги в **World Art**_ при _поиске связанной с anime манги_
  (`world_art.manga_pages_from_anime.related_manga`) теперь ищет ссылку на мангу в третьей ячейке строки таблицы, если
  во второй не найдена.
* _Извлечение наименования из поискового ответа **ANN**_ При _первичном поиске XML-страниц манги и anime в **ANN**_
  (`animenewsnetwork.search_pages.pars`) теперь учитывает возможное отсутствие в тексте примечаний в скобках.
* При _поиске и получении XML-страниц манги и anime в **ANN** на базе уже полученных XML-страниц_
  (`animenewsnetwork.pages`):
  * _обработка XML-страницы anime, получение и добавление в соответствующие словари связанных XML-страниц_
    (`animenewsnetwork.pages.apages`) теперь ищет `alternate` в атрибуте `rel` тегов `related-prev` и `related-next`,
    если не найдена `adapt`;
  * _обработка XML-страницы манги, получение и добавление в соответствующие словари связанных XML-страниц_
    (`animenewsnetwork.pages.mpages`) теперь:
    * ищет `alternate` в атрибуте `rel` тегов `related-prev` и `related-next`, если атрибут `rel` не содержит
      `adaptation` или `adapted`;
    * найдя в атрибуте `rel` `adaptation` или `adapted`, или `alternate`, но не найдя ID (из атрибута `id`) в словаре
      уже полученных anime, не переходит к поиску ID в словарях уже полученной манги.
* При инициализации класса _страницы из **Wikipedia** (en)_ (`wikipedia_.Page`) теперь в конце проверяется наличие
  в тексте страницы строки `anime and manga`. Если строка не найдена, то страница и её адрес аннулируются (`None`).
* _Поиск страниц по наименованию в **Wikipedia** (en)_ (`wikipedia_.search_pages`) теперь обрабатывает подзаголовок
  `Spin-offs`, а также возможное отсутствие тега `ul`.
* _Получение HTML-страниц из **Wikipedia** (en) по ссылкам в страницах **World Art**_ (`wp_pages_in_wa`) теперь
  учитывает разную структуру страниц anime и манги.
* _Извлечение заголовка anime в **Wikipedia** (en) по ссылке в **World Art**_ (`world_art.wp_anime_title` (бывшая
  `wp_title`)) теперь учитывает возможное отсутствие строки ссылок.
* _Извлечение из страницы частей страницы (частей инфо-блоков) по отдельным манге и anime в **Wikipedia** (en)_
  (`wikipedia_.manga_anime_in_page`) теперь ищет в подзаголовках частей страницы помимо английского наименования ещё
  и оригинальное и ромадзи.
* _Фильтр частей страниц, удаляющий повторы, и переформатирование словаря частей страниц_
  (`wikipedia_.filter_page_parts`) теперь в словарном ключе-заголовке части страницы строку `(Anime television series)`
  заменяет на `(TV)`.
* _Извлечение оригинального наименования из страницы в **World Art**_ (`world_art.title_orig`) теперь проверяет все
  наименования (яп., кандзи, прочие) и, найдя более одного, предлагает пользователю выбрать.
* _Извлечение ромадзи наименования из страницы в **World Art**_ (`world_art.title_rom`) теперь, не найдя `ромадзи`, ищет
  `яп.`.
* _Извлечение изданий из страницы в **World Art**_ (`world_art.publications`) теперь:
  * найдя издательство `Kadokawa`, переименовывает его в `Kadokawa Shoten`;
  * найдя издание `Manga Action` (издательства `Futabasha`), переименовывает его в `Shuukan Manga Action`.
* _Извлечение режиссёров из страницы в **World Art**_ (`world_art.directors`) теперь не пропускает (ошибочно) последнего
  в списке режиссёра.
* _Извлечение издания манги из XML-страницы в **ANN**_ (`animenewsnetwork.publications` (бывшая `publication`)) теперь,
  не найдя тег `related-prev` с атрибутом `rel='serialized in'`, ищет тег `info` с атрибутом `type='Vintage'`.
* _Извлечение наименования издательства_ (`animenewsnetwork.publications.publishing`) теперь:
  * принимает параметр `f_id` — _флаг возвращения ID издательства_ (`bool`);
  * если ещё отсутствует HTML-страница издания, запрашивает HTML-страницу манги;
  * если включён флаг возвращения ID издательства, то возвращает кортеж, вторым элементом которого возвращает ID
    издательства.
* _Извлечение количества эпизодов anime из XML-страницы в **ANN**_ (`animenewsnetwork.number_of_episodes`) теперь
  проверяет наличие пробела в тексте тега и, найдя его, обрезает текст по пробелу.
* _Извлечение количества томов манги из соответствующей части инфо-блока в **Wikipedia** (en)_
  (`wikipedia_.number_of_volumes`) теперь проверяет наличие пробела в тексте ячейки и, найдя его, обрезает текст
  по пробелу.
* _Извлечение наименований издательства и издания в **Wikipedia** (en)_ (`wikipedia_.publications`) теперь проверяет
  наличие запятой в тексте ячейки издания и, найдя её, режет строку по запятым и преобразует в список.
* _Извлечение формата anime из соответствующей части инфо-блока в **Wikipedia** (en)_ (`wikipedia_.anime_format`)
  теперь, не найдя формата в первой строке части страницы, ищет формат в следующих подзаголовках части страницы.
* _Извлечение количества эпизодов anime из соответствующей части инфо-блока в **Wikipedia** (en)_
  (`wikipedia_.number_of_episodes`) теперь проверяет наличие пробела или/и запятой в тексте ячейки и, найдя, обрезает
  текст по пробелу или/и запятой.
* _Формирование единого словаря данных из словарей данных, извлечённых из страниц_ (`data_join`) теперь:
  * при сравнении _наименований (ромадзи)_ манги или anime в **World Art** и в **ANN** заменяет `'-hen'` на `' hen'`
    и удаляет `-`;
  * _сверка имён (ромадзи и оригинальных) авторов манги_ (`data_join.maut`) при сравнении оригинальных имён удаляет
    пробелы;
  * если _года премьеры_ манги в **World Art** и в **ANN** совпадают, но _даты премьеры_ отличаются, выбирает дату
    **ANN**.

### Версия 0.6.12 20260225
- В модуле _константы_ (`constants`) удалён словарь _заголовков_ (`HEADERS`).

+ В модуле _конфигурации_ (`config`) добавлена _версия операционной системы Windows_ (`WINDOWS_VER`).
  Значение «из пакета» — `'11'`.
+ В модуле _константы_ (`constants`)
  + добавлен _адрес поискового запроса для **Wikipedia** (en)_
    `WPE_SEARCH = 'https://en.wikipedia.org/w/index.php?title=Special%3ASearch&fulltext=1&ns0=1&search='`;
  * в `USER_AGENT` версия Windows теперь задана константой `config.WINDOWS_VER`.
+ Добавлен модуль _работы с временными файлами_ (`file_cache`), в который добавлены
  + функция _варианта ручного обхода анти-бот-защиты_ (`file_cache.anti_bot`),
  + класс _кэша новых жанров, которые пользователю желательно перенести из TXT-файла в «config.py» после завершения
    работы кэша_ (`file_cache.NewGenre`).

* Модуль _поиска страниц в **World Art** и их обработки_ (`world_art`) полностью переписан с использованием пакета
  `BeautifulSoup` (`bs4`).
* _Получение HTML-кода страницы в **World Art**_ теперь ответ проверяет на соответствие статус-кода интервалу [200, 400)
  и, в случае успеха, контент ответа прогоняет через HTML-парсер BeautifulSoup. (`world_art.html`)
* Модуль _поиска страниц в **AnimeNewsNetwork** и их обработки_ (`animenewsnetwork`) полностью переписан
  с использованием пакетов `xml` (модуля `xml.etree.ElementTree`) и `BeautifulSoup` (`bs4`).
* Функция _страница (HTML-код) в **ANN**_ (`animenewsnetwork.html`) теперь
  * третьим параметром принимает _флаг включения расширения «.php» в адрес_ (`_php`) булевского типа со значением
    по-умолчанию `True`;
  * сформированную ссылку передаёт выделенной функции _варианта ручного обхода анти-бот-защиты_ (`file_cache.anti_bot`),
    ответ которой прогоняет через HTML-парсер BeautifulSoup.
* Функция _XML-страница манги или anime в **ANN**_ (`animenewsnetwork.xml`) теперь сформированную ссылку передаёт
  выделенной функции _варианта ручного обхода анти-бот-защиты_ (`file_cache.anti_bot`), ответ которой преобразовывает
  в объект `xml.etree.ElementTree.Element`.
* Класс _страницы из **Wikipedia** (en)_ (`wikipedia_.Page`) теперь использует константу _адрес поискового запроса
  для **Wikipedia** (en)_ (`constants.WPE_SEARCH`) и _вариант ручного обхода анти-бот-защиты_ (`file_cache.anti_bot`),
  а также HTML-парсер BeautifulSoup для поиска искомого наименования в **Wikipedia** (en).
* _Извлечение издания манги из XML-страницы в **ANN**_ (`animenewsnetwork.publication`) теперь
  * не принимает первым параметром _ID манги в **ANN**_: параметр удалён; а
  * проверка наименования найденного издания на наличие слова `USA` упразднена.
* _Извлечение жанров из XML-страницы в **ANN**_ (`animenewsnetwork.genres`) теперь использует класс _кэша новых жанров_
  (`file_cache.NewGenre`).

### Версия 0.6.11 20260202
- Отключено (временно) использование сервиса **MangaUpdates**.
- Класс _страницы из **Wikipedia** (en)_ больше не использует пакет `wikipedia`. (`wikipedia_.Page`)
  Если и вернётся, то нескоро.
- _Извлечение наименования (англ.) из соответствующей части инфо-блока в **Wikipedia** (en)_
  (`wikipedia_.title`) удалена, т.к. теперь используется наименование, извлечённое при _извлечении из страницы частей
  страницы (частей инфо-блоков) по отдельным манге и anime в **Wikipedia** (en)_ (`wikipedia_.manga_anime_in_page`).
- _Извлечение количества глав манги из страницы манги в **Wikipedia** (en)_ (`wikipedia_.number_of_chapters_2`) удалена,
  а логика перенесена в основную функцию _извлечения количества глав манги из соответствующей части инфо-блока
  в **Wikipedia** (en)_ (`wikipedia_.number_of_chapters`).
 
+ Ко времени завершения разработки данной версии на **ANN** введена система анти-бот-защиты.
  В данной версии добавлен пробный метод обхода: пользователю предлагается самостоятельно открыть страницу
  (предоставляется ссылка) в браузере, открыть исходный код страницы и скопировать код во временный файл,
  который программа и прочитает. (`animenewsnetwork.html`)
+ В модуле _константы_ (`constants`) добавлен словарь _заголовков_ (`HEADERS`).
+ В модуле _конфигурации_ (`config`)
  + добавлен словарь _куки **Wikipedia** (en)_ `COOKIES_WP`, требующий частой актуализации;
  + к _игнорируемым жанрам_ (`IGNORED_GENRES`) добавлен `Tournament`;
  * обновлена _версия Firefox для User-Agent_ до `147.0`.
+ Добавлена функция _извлечения оригинального наименования манги из соответствующей части инфо-блока в **Wikipedia**
  (en)_. (`wikipedia_.title_orig`)
+ Добавлена функция _извлечения ссылки на постер из соответствующей части инфо-блока в **Wikipedia** (en)_.
  (`wikipedia_.poster`)

* _Декодирование символов Юникод и шестнадцатеричных кодов в наименовании_ теперь декодирует именованные и номерованные
  символьные ссылки (коды) через `html.unescape`. (`decode_name.decode_name`)
* Оптимизирована функция _первичного поиска XML-страниц манги и anime в **ANN**, передачи результатов в функцию pages
  и возврата её результатов_. (`animenewsnetwork.search_pages`)
* При _обработке XML-страницы anime_ в _поиске и получении XML-страниц манги и anime в **ANN** на базе уже полученных
  XML-страниц_ теперь пропускаются уже обработанные ссылки. (`animenewsnetwork.pages.apages`)
* Модуль _поиска страниц в **Wikipedia** (en) и их обработки_ (`wikipedia_`) полностью переписан с использованием пакета
  `BeautifulSoup` (`bs4`). (В следующих версиях планируется переписать с использованием пакета `BeautifulSoup` (`bs4`)
  модули `world_art` и `animenewsnetwork`.)
* _Получение HTML-кода страницы по внутренней ссылке в **Wikipedia** (en)_ (`wikipedia_.page`) теперь:
  * для обхода анти-бот-защиты использует заголовки (`constants.HEADERS`) и куки (`config.COOKIES_WP`);
  * принимает второй параметр `_text` — переключатель режима возврата функции:
    * `True` — текст ответа (`requests.Response.text`),
    * `False` (по-умолчанию) — контент ответа (`requests.Response.content`) для обработки пакетом `BeautifulSoup`.
* _Извлечение заголовка в **Wikipedia** (en) по ссылке в **WA**_ (`world_art.wp_title`) теперь
  * учитывает, что ссылка может быть заключена как в `'`, так и в `"`;
  * возвращаемую строку прогоняет через `urllib.parse.unquote` для декодирования `%xx`-кодов.
* _Фильтр частей страниц, удаляющий повторы, и переформатирование словаря частей страниц_
  (`wikipedia_.filter_page_parts`)
  * больше не добавляет дату премьеры в скобках к наименованию части страницы;
  * сама часть страницы теперь прописывается в словарь как строковый элемент с индексом-наименованием:
    `{'manga' | 'anime': {title: page_part}}` — `dict[str, dict[str, str]]`, а не как словарь, в котором помимо самой
    части страницы содержался адрес (краткий) страницы (`{'manga' | 'anime': {title: {'page_part': page_part,
    'page_title': page_title}}}` — `dict[str, dict[str, dict[str, str]]]`).
* _Извлечение наименования из XML-страницы в **ANN**_ теперь декодирует именованные и номерованные символьные ссылки
  (коды) во возвращаемой строке через `html.unescape`. (`animenewsnetwork.title`)
* Подправлен алгоритм _извлечения авторов манги или режиссёров anime из XML-страницы в **ANN**_.
  (`animenewsnetwork.authors`)
* _Извлечение издания манги из XML-страницы в **ANN**_ теперь проверяет наименование найденного издания на наличие слова
  `USA`. При положительном результате проверки поиск издания продолжается. (`animenewsnetwork.publication`)
* _Извлечение данных из частей инфо-блоков в **Wikipedia** (en)_ больше не принимает вторым параметром словарь
  HTML-страниц из **Wikipedia** (en). (`wikipedia_.extraction_data`)
* _Извлечение данных по манге из соответствующей части инфо-блока в **Wikipedia** (en)_ больше не принимает вторым
  параметром словарь HTML-страниц из **Wikipedia** (en). Теперь вторым параметром принимает наименование страницы.
  (`wikipedia_.extraction_manga`)
* _Извлечение наименований издательства и издания в **Wikipedia** (en)_ теперь декодирует наименование издания
  с помощью `decode_name.decode_name`. (`wikipedia.publications`)
* _Извлечение студий anime из соответствующей части инфо-блока в **Wikipedia** (en)_ теперь декодирует наименование
  студии с помощью `decode_name.decode_name`. (`wikipedia.studios`)
* Исправлена ошибка в методе `add` класса `DB`. (`db.DB.add`)

### Версия 0.6.10 20251202
* При _обработке XML-страницы anime_ в _поиске и получении XML-страниц манги и anime в **ANN** на базе уже полученных
  XML-страниц_ теперь пропускаются ссылки на тематические циклы такие как «World Masterpiece Theater»
  (ссылка обозначается как `rel="part of"`). (`animenewsnetwork.pages.apages`)
* _Извлечение заголовка в **Wikipedia** (en) по ссылке в **WA**_ теперь ищет ссылки и с протоколом `HTTPS`.
  (`world_art.wp_title`)
* _Формирование единого словаря данных из словарей данных, извлечённых из страниц_ теперь перед возвратом единого
  словаря данных удаляет корневые элементы `manga` и/или `anime`, если они содержат пустой список. (`data_join`)
* _Устранение одноимённости разных anime и манги_ теперь итерирует не постоянный кортеж, а ключи единого словаря данных.
  (`title_unique`)
* _Вывод единого словаря данных пользователю и запрос указания добавляемых в БД данных_ теперь учитывает возможное
  отсутствие манги или anime (корневых ключей). (`control`)
* _Добавление данных в БД_ теперь учитывает возможное отсутствие манги или anime (корневых ключей).

### Версия 0.6.9 20251128
+ В модуле _константы_ (`constants`) добавлены следующие:
  + `WPES = 'https://en.wikipedia.org/wiki/'`,
  + `USER_AGENT = f'Mozilla/5.0 (Windows NT 10.0; Win64; x64; rv:{FIREFOX_VER}) Gecko/20100101 Firefox/{FIREFOX_VER}'`
    (указан браузер Mozilla Firefox в ОС Windows 10, версия которого указывается в модуле конфигурации).
+ В модуле _конфигурации_ (`config`)
  + добавлена версия Firefox для User-Agent: `FIREFOX_VER = '145.0.2'` (рекомендуется к актуализации);
  + к _WA-форматы anime в ANN_ добавлен `{'special': 'ТВ-спэшл'}`;
  + к _игнорируемым жанрам_ добавлены `история`, `Harem` и `Historical`;
  + к _жанрам AnimeNewsNetwork_ добавлен `{'magic': 'магия'}`.
+ При импортировании модуля _поиска страниц в **Wikipedia** (en) и их обработки_ (`wikipedia_`) происходит
  + переключение `API_URL` в пакете `wikipedia` на протокол `HTTPS`,
  + установка `User-Agent` в пакете `wikipedia` в значение из константы `USER_AGENT`.
+ Добавлен вывод в консоль _контрольной информации о текущей выполняемой функции_. (Когда обрабатывается франшиза,
  может показаться, что программа зависла (бесконечный цикл либо сетевые блокировки).)
+ В модуле _поиска страниц в **Wikipedia** (en) и их обработки_ выделены функции
  + _поиска инфо-бокса_ (`wikipedia_._pos_table`),
  + _поиска текста между HTML-тегами_ (`wikipedia_._pos_ab`).
+ Добавлена проверка наличия наименований **ANN** и **Wikipedia** (en) в **WA**: поиск и добавление отсутствующих.
  А также извлечение данных из вновь найденных страниц.
  + Выделена функция _обновления словарей HTML-страниц anime и манги из **WA** текущими данными_. (`wa_pages_update`)

* _Полученный HTML-код страницы в **WA**_ теперь сразу проходит декодирование символов. (`world_art.html`)
* _Декодирование символов_ теперь происходит не только Юникод, но и шестьнадцатиричных кодов. (`decode_name.decode_name`)
* Поиск связей anime и манги в **WA** теперь учитывает ведущий ноль в номере в номерованном списке.
  (`world_art.anime_pages`, `world_art.manga_pages`, `world_art.manga_pages_from_anime`, `world_art.related_anime`)
* _Нормализация наименования_ теперь производит
  * декодирование символов и
  * замену дефисов пробелами.

  (`decode_name.normal_name`)
* _Первичный поиск XML-страниц манги и anime в **ANN**_ теперь отфильтровывает ссылки на XML-страницы новелл.
  (`animenewsnetwork.search_pages`)
* _Поиск и получение XML-страниц манги и anime в **ANN** на базе уже полученных XML-страниц_ теперь
  * больше не ищет и не загружает XML-страницы повторно,
  * обрабатывает случаи отсутствия XML-страниц anime в БД **ANN** (`no result`).

  (`animenewsnetwork.pages`)
* _Первичный поиск данных по манге в **MangaUpdates**_ теперь обрабатывает ошибку подключения: в случае возникновения
  выдаёт уведомление в консоль и возвращает `None`. (`mangaupdates.search_pages`)
* _Получение HTML-кода страницы по внутренней ссылке в **Wikipedia** (en)_ теперь
  * происходит по протоколу `HTTPS` с указанием `User-Agent` в заголовке запроса, который указывается в модуле констант
    (`constants.USER_AGENT`) и рекомендуется к актуализации;
  * результат декодируется.

  (`wikipedia_.page`)
* Функция _получения страницы (HTML-кода) из **Wikipedia** (en)_ заменена классом _страницы из **Wikipedia** (en)_.
  При инициализации объекта происходит
  1. поисковый запрос (`wikipedia.search`),
  2. поиск искомого наименования среди результатов поискового запроса,
  3. получение страницы по выбранному результату поискового запроса (`wikipedia.WikipediaPage`),
  4. присваивание свойству `html` декодированного HTML-кода страницы,
  5. присваивание свойству `url` фрагмента URL-адреса (после `/wiki/`), содержащего заголовок страницы.

  Если среди результатов поискового запроса искомого наименования нет, тогда происходит переключение на функцию `page`.
  Если сервер **Wikipedia** (en) сообщает об ошибке (`wikipedia.WikipediaException`) 15 раз подряд (около 15 секунд),
  также происходит переключение на функцию `page`. Результат выполнения функции `page` присваивается свойству `html`,
  а её параметр — свойству `url`. (`wikipedia_.Page`)
* _Поиск страниц по наименованию в **Wikipedia** (en)_ полностью переписан.
  * Вторым параметром функция принимает необязательный дополняемый словарь `{title: HTML}` страниц в **Wikipedia** (en).
    В этот словарь дописываются найденные страницы, после чего он возвращается. (Цель такой схемы — устранение
    повторного поиска и загрузки.)
  * В качестве ключей результирующего словаря прописываются заголовки страниц из `Page.url`.
  * В инфо-боксе обрабатываются списки `Related series`, `Feature films` и `Related works`. 

  (`wikipedia_.search_pages`)
* _Извлечение из страницы частей страницы (частей инфо-блоков) по отдельным манге и anime в **Wikipedia** (en)_ почти
  полностью переписано. (`wikipedia_.manga_anime_in_page`)
* Оптимизирован код _извлечения даты премьеры из части страницы в **Wikipedia** (en)_. (`wikipedia_.date_of_premiere`)
* _Извлечение оригинального наименования из страницы в **WA**_ теперь не декодирует результат. (`world_art.title_orig`)
* _Извлечение ромадзи наименования из страницы в **WA**_ теперь не декодирует результат. (`world_art.title_rom`)
* _Извлечение английского наименования из страницы в **WA**_ теперь не декодирует результат. (`world_art.title_eng`)
* _Извлечение русского наименования из страницы в **WA**_ теперь не декодирует результат. (`world_art.title_rus`)
* _Извлечение имён персоны из страницы в **WA**_ теперь не декодирует оригинальное имя. (`world_art.people`)
* _Извлечение наименования из XML-страницы в **ANN**_ почти полностью переписано. (`animenewsnetwork.title`)
* _Извлечение авторов манги или режиссёров anime из XML-страницы в **ANN**_ теперь учитывает блоки
  `<div id="infotype-13">` и `<div id="infotype-12">`. (`animenewsnetwork.authors`)
* _Извлечение издания манги из XML-страницы в **ANN**_ теперь учитывает блок `<div id="infotype-2">`.
  (`animenewsnetwork.publication`)
* После _извлечения данных anime из XML-страницы в **ANN**_ в ромадзи наименование прописывается английское
  наименование, если ромадзи наименование совпадает с оригинальным наименованием и отличается от английского
  наименования. (`animenewsnetwork.extraction_anime`)
* _Извлечение формата anime из соответствующей части инфо-блока в **Wikipedia** (en)_ теперь не определив формата,
  но обнаружив в наименовании слова `movie` (регистр не важен), возвращает `FORM_WP['Anime film series']` (соответствует
  WA-формату `полнометражный фильм`). (`wikipedia_.anime_format`)
* _Извлечение количества эпизодов anime из соответствующей части инфо-блока в **Wikipedia** (en)_ помимо поля `Episodes`
  теперь учитывается поле `<abbr title="Number">No.</abbr> of episodes`. (`wikipedia_.number_of_episodes`)
* _Извлечение студий anime из соответствующей части инфо-блока в **Wikipedia** (en)_ теперь обрабатывает список студий,
  оформленный только тегом `<br />`. (`wikipedia_.studios`)
* _Извлечение имён авторов манги или режиссёров anime из соответствующей части инфо-блока в **Wikipedia** (en)_ почти
  полностью переписано. (`wikipedia_.authors`)
* _Извлечение наименований издательства и издания в **Wikipedia** (en)_ теперь не добавляет в результирующий список
  издание, если оно там уже имеется. (`wikipedia_.publications`)
* В модуле _поиска страниц в **MangaUpdates** и их обработки_ _выбор наименования манги через обращение к пользователю_
  теперь уточняет у пользователя и ромадзи наименование, а не берёт его из заголовка (`'title'`) в JSON-ответе.
  (`mangaupdates.select_title`)
* При _формировании единого словаря данных из словарей данных, извлечённых из страниц_ (`data_join`) теперь
  * обрабатываются числовые ключи, взятые из JSON-словаря в строковом формате;
  * в единый словарь данных записываются ID манги в **WA** и **ANN**;
  * поиск соответствующей манги или anime в **ANN**
    * в первую очередь производится по ID, затем по дате премьеры (более детально), ромадзи наименованию и авторам;
    * при обработке найденной манги в единый словарь данных записывается ID манги в **ANN**, а поиск прекращается;
  * в _сверке имён (ромадзи и оригинальных) авторов манги_ (`data_join.maut`) в качестве параметра принимается список
    словарей имён авторов сравниваемой манги;
  * в _поиске соответствующей манги в словаре **Wikipedia** (en)/**MangaUpdates** и дополнении недостающими данными
    единого словаря данных по манге_ (`data_join.mwp`/`data_join.mmu`)
    * _даты премьеры_ сравниваются более детально, а также сравниваются _наименования_, а найдя — поиск не продолжается;
    * _издания_ добавляются только в пустой список;
  * величина погрешности в указании года премьеры манги в **MangaUpdates** относительно **ANN** равна `1`;
  * в _поиске соответствующего anime в словаре **Wikipedia** (en) и дополнении недостающими данными единого словаря
    данных по anime_ (`data_join.awp`)
    * исправлена ошибка отсутствия _продолжительности_ anime в едином словаре данных;
    * если _даты премьеры_ или _режиссёры_ не совпадают, сравниваются _наименования_ и _годы_ из дат премьер;
    * если найден _режиссёр_, отсутствующий в едином словаре данных, перед его добавлением проверяется наличие русского
      имени и, при отсутствии, выполняется запрос в WA по ромадзи имени;
    * найдя соответствующее anime, поиск не продолжается;
  * в _замене None в обязательных полях на значения по умолчанию (или заглушки)_ (`data_join.not_none`) добавлена
    заглушка `—<Нет информации>—` для списка _студий_;
  * поиск в едином словаре данных манги, связанной с anime, производится по ID в соответствующей БД
    (**WA** или **ANN**);
  * исправлена ошибка отсутствия _студии_ anime в **ANN**.

### Версия 0.6.8 20251007
+ Выделена отдельная функция-сокращение записи замены символов `ō` и `ū` на `ou` и `uu`. Используется не только
  в нормализации наименований, но и для коррекции наименований изданий. (`decode_name.o_ou`)
* Исправлено извлечение даты премьеры манги из XML-страницы в **ANN**. (`animenewsnetwork.manga_date_of_premiere`)
* Исправлено извлечение из страницы частей страницы (частей инфоблоков) по отдельным манге и anime в **Wikipedia** (en).
  (`wikipedia_.manga_anime_in_page`)
* Изменена структура словаря, возвращаемого при фильтрации частей страниц и переформатировании словаря частей страниц
  в **Wikipedia** (en). Теперь вместе с частью страницы сохраняется заголовок страницы, который потребуется при выборе
  страницы в функции извлечения данных из частей инфоблоков в **Wikipedia** (en). (`wikipedia_.filter_page_parts`,
  `wikipedia_.extraction_data`)
* Исправлено извлечение издания манги из XML-страницы в **ANN**. А также учтена ошибка **ANN**:
  издания `Monthly Shōnen Sunday` (`Gekkan Shounen Sunday`) у издательства `Kodansha` не было. Правильное наименование —
  `Gekkan Shounen Magazine`. (`animenewsnetwork.publication`)
* Исправлено извлечение количества глав манги из страницы манги в **Wikipedia** (en).
  (`wikipedia_.number_of_chapters_2`)
* Доработана функция извлечения наименований издательства и издания в **Wikipedia** (en). (`wikipedia_.publications`)

### Версия 0.6.7 20250930
* В функции извлечения изданий из страницы в **WA** добавлена замена наименования издания `Morning`
  на `Shuukan Morning`. (`world_art.publications`)
* Доработано извлечение количества глав манги из страницы манги в **Wikipedia** (en), когда приводится список глав,
  а не томов. (`wikipedia_.number_of_chapters` и `wikipedia_.number_of_chapters_2`)
  * Отменена оптимизация кода поиска страниц в **Wikipedia** (en), добавленная в прошлой версии (`search_wp_pages`),
    и переработана функция получения HTML-страниц из **Wikipedia** (en) по ссылкам в страницах **WA**
    (`wp_pages_in_wa`).
  * Добавлен соответствующий параметр (`_pages`) в функциях извлечения данных из частей инфоблоков в **Wikipedia** (en)
    (`wikipedia_.extraction_data`) и извлечения данных по манге из соответствующей части инфоблока в **Wikipedia** (en)
    (`wikipedia_.extraction_manga`).
* Переработана функция извлечения наименований издательства и издания в **Wikipedia** (en). (`wikipedia_.publications`)
  * Внесена соответствующая поправка в функцию формирования единого словаря данных из словарей данных, извлечённых
    из страниц. (`data_join`)
* В функции извлечения изданий манги из полного JSON-словаря данных по манге в **MangaUpdates**. добавлена замена
  наименований изданий: `Morning` на `Shuukan Morning` и `Afternoon` на `Gekkan Afternoon`.
  (`mangaupdates.publications`)
  * Отменено изменение прошлой версии: при формировании единого словаря данных учтено возможное отсутствие слова
    `Shuukan` в наименовании издания в JSON-словаре **MangaUpdates**. (`data_join.mmu`)
* Переработан поиск и дополнение недостающими данными по _изданиям_ единого словаря данных по манге. (`data_join.mwp`)
* В словарь жанров **MangaUpdates** добавлен `{'Ecchi': 'этти'}`. (`config`)

### Версия 0.6.6 20250926
* Переработана функция получения данных по манге в **MangaUpdates**. Теперь таймаут сервера не вызывает ошибку,
  а производится ещё одна попытка (всего 5 попыток). Если нет результата — возвращается `None`.
  (`mangaupdates.manga_json`) Соответственно изменена функция получения данных по манге из **MangaUpdates**,
  включая по связанной манге: также возвращает `None`, когда `manga_json` возвращает `None`.
  (`mangaupdates.search_pages_id`)
* Оптимизация кода поиска страниц в **Wikipedia** (en). (`search_wp_pages`)
* Исправлено извлечение даты премьеры манги из XML-страницы в **ANN**. (`animenewsnetwork.manga_date_of_premiere`)
* Исправлена и оптимизирована функция извлечения оригинального и ромадзи имён из HTML-страницы автора в **Wikipedia**
  (en). (`wikipedia_.authors.orig_rom`)
* Исправлена ошибка при чтении файла `new_genres.txt` при извлечении жанров манги из полного JSON-словаря данных
  по манге в **MangaUpdates**. (`mangaupdates.genres`)
* Переработана функция поиска персоны в **WA**. (`world_art.search_people`)
* При формировании единого словаря данных учтено возможное отсутствие слова `Shuukan` в наименовании издания
  в JSON-словаре **MangaUpdates**. (`data_join.mmu`)
* В функции устранения одноимённости (`title_unique`) удалена нормализация оригинального наименования в сравнении.
* В словарь жанров **MangaUpdates** добавлен `{'Martial Arts': 'боевые искусства'}`. (`config`)

### Версия 0.6.5 20250919
+ При выборе пользователем добавляемых в БД данных, добавлена возможность указать всё сразу, введя `*`. (`control`)
+ В результате выбора пользователем добавляемых в БД данных единый словарь данных для сохранения теперь не сокращается
  (это нарушало алгоритм последующей обработки). В единый словарь данных для сохранения добавляется параметр `save`,
  булевое значение которого сигнализирует о выборе пользователя.
* Исправлено получение данных по манге из БД **MangaUpdates**. (`mangaupdates.search_pages_id`)
* Оптимизировано извлечение авторов манги из полного JSON-словаря данных по манге в **MangaUpdates**.
  (`mangaupdates.authors_of_manga`)
* Исправлен поиск в БД студии, если наименование равно `Toei Animation`. Т.к. ранее студия меняла наименование, в БД
  сохранены сразу оба: `Toei Animation (Toei Doga)`. (`db.get_id`)

### Версия 0.6.4 20250914
* Исправлена пара ошибок в модуле формирования контрольной таблицы (Table).
* Из модуля БД (db) удалён лишний код, вызывающий ошибку.
* В словарь жанров **MangaUpdates** добавлен {'Mecha': 'меха'} (config).

### Версия 0.6.3 20250906
+ Добавлен поиск персоны в **WA** по имени ромадзи или английскому для добавления русского имени, если его нет в
  других БД.
+ Добавлен вывод в консоль итоговых данных и выбор пользователем добавляемых в БД данных.
+ Добавлена проверка наличия в БД anime и манги по наименованию (оригинальному и ромадзи). Anime и манга будут добавлены
  в БД только если нет соответствующего наименования.
* Доработано извлечение даты премьеры из части страницы в **Wikipedia** (en).
* Доработано извлечение автора манги из JSON-словаря **MangaUpdates**.
* Доработано извлечение издательства из JSON-словаря **MangaUpdates**.
* При добавлении манги в БД, поиск издания в БД теперь происходит не по вхождению, а по равенству.

### Версия 0.6.2 20250824
* Доработано извлечение частей страниц в **Wikipedia** (en).
* Изменено извлечение даты премьеры из части страницы в **Wikipedia** (en).
* Исправлена ошибка в сравнении данных из **WA** с данными из **ANN**.

### Версия 0.6.1 20250821
* Исправлена ошибка при попытке извлечь дату премьеры манги из XML-страницы в **ANN**, когда отсутствует соответствующий
  элемент.
* Доработан фильтр поиска страниц в **Wikipedia** (en).
* Исправлена ошибка при попытке извлечения данных из JSON-словаря в **MangaUpdates**, если он отсутствует.
* Исправлена ошибка, возникающая из-за отсутствующих значений обязательных полей. Добавлена функция заполнения этих
  полей значениями по умолчанию или заглушками.
* Исправлена ошибка извлечения отсутствующего издания из страницы в **Wikipedia** (en).
* Добавлен фильтр HTML-тегов в оригинальном имени автора в **Wikipedia** (en).

### Версия 0.6.0 20250820
* Полностью заменён основной алгоритм работы (см. `README.md`).
* Теперь не используется web-интерфейс PHP-приложения, а используется прямое подключение к БД через библиотеку
  `mysqlclient`.

### Версия 0.5.2 20250116
* Исправление ошибок и оптимизация кода перед разработкой версии 0.6.0.

### Версия 0.5.1 20250115
* Скорректирован поиск манги в инфобоксе страницы в **Wikipedia** (en).
* Извлечение даты премьеры манги из **ANN**. Теперь учитывается, если указана дата в формате `yyyy-mm`.
* Если при извлечении манги из **WA** русское наименование манги совпало с ромадзи и определена манга
  в **MangaUpdates**, **пользователю** предоставляется выбор русского наименования из списка,
  извлечённого из **MangaUpdates**.
* В извлечение манги из **MangaUpdates** (обработка глобального списка) добавлено извлечение русского наименования.

### Версия 0.5.0 20250113
+ Доработан поиск манги в инфобоксе страницы в **Wikipedia** (en).
+ Заменён алгоритм извлечения манги из **ANN** (обработка глобального словаря).
    + Расширен поиск манги в **MangaUpdates**. В случае неудачного поиска возвращается `None`.
    + Ромадзи наименование извлекается из **MangaUpdates**, либо из **ANN**.
    + Поиск в **Wikipedia** (en) производится по извлечённому ромадзи наименованию,
      либо по наименованию из входных данных.
    + Если манга найдена в **MangaUpdates**, производится попытка извлечения изданий манги из **MangaUpdates**.
    + Если в **MangaUpdates** манга не найдена, то производится попытка извлечения изданий манги из **Wikipedia** (en).
    + Издания из **Wikipedia** (en) теперь извлекаются списком.
    + Если издание в **Wikipedia** (en) не найдено, то оно извлекается из **ANN** вместе с датой премьеры.
    + Если манга найдена в **MangaUpdates**, производится попытка извлечения жанров из **MangaUpdates**.
      Иначе жанры извлекаются из **ANN**.
    + В файл конфигураций добавлен словарь жанров **ANN** `GENRES_ANN`.
    + Если жанры не найдены, пользователю предлагается выбрать их из БД.
    + Если из **ANN** дата не извлечена, то она извлекается из **Wikipedia** (en).
    + Проверяется извлечённая дата. Если её нет или оканчивается на `-12-31`, то в примечании отмечается отсутствие
      даты премьеры.
    + Если дата так и не найдена, прописывается «заглушка» `1900-01-01`.
    + Английское наименование извлекается из **ANN**, либо предоставляется пользователю выбор из списка,
      полученного из **MangaUpdates**.
    + Количество томов извлекается из **ANN**, либо из **MangaUpdates** (если манга найдена в **MangaUpdates**),
      либо прописывается `1` и в примечании отмечается отсутствие информации о количестве томов.
    + Из **ANN** извлекаются авторы манги.
    + Из **ANN** извлекается оригинальное наименование.
    + Формируется строка примечания.
    + Если оригинальное наименование пусто (не найдено), то в него прописывается ромадзи.
+ При извлечении манги из **MangaUpdates** (обработка глобального списка) извлекается либо количество томов,
  либо количество глав (когда том ещё не издавался). Во втором случае в примечании выключается указание о главах.
* Исправлено извлечение даты премьеры манги из **Wikipedia** (en), когда указаны месяц и год без числа (дня).
* Извлечение оригинального наименования из **WA** дополнено проверкой поля `Названия (яп.)`.
* Исправлено извлечение наименования из **ANN** и добавлено извлечение ромадзи наименования из заголовка.
* При извлечении anime из **WA**, если оригинальное наименование совпало с русским (не нашлось оригинальное),
  а ромадзи не пусто (нашлось), тогда в оригинальное наименование прописывается ромадзи.
* При извлечении из **WA**, если не найдено английское наименование (ни в WA, ни в ANN),
  а оригинальное совпадает с ромадзи, тогда в английское наименование прописывается ромадзи.

### Версия 0.4.0 20250111
+ Добавлен поиск манги в **ANN**, когда нет ссылки на странице манги в **WA**.
  Используется страница anime в **ANN** через ссылку на странице anime в **WA**.
+ Добавлен поиск связанной манги в **MangaUpdates** через ссылку на странице манги в **WA**,
  если поиск по наименованию дал пустой результат.
+ Добавлен поиск даты премьеры манги в **ANN**, когда манга отсутствует в **Wikipedia** (en).
* К игнорируемым жанрам добавлен дзёсэй.

### Версия 0.3.3 20250110
* Исправлена ошибка в функции поиска постера.

### Версия 0.3.2 20250110
* Возвращено сохранение данных по манге из **WA**. Было временно выключено при разработке версии 0.3.1,
  но забыто при публикации.
* Исправлено добавление комментария об отсутствии точной даты премьеры при добавлении манги из **WA**.
* Исправлена ошибка в функции поиска постера.
* Исправлен поиск даты премьеры манги в **Wikipedia** (en) для случаев,
  когда искомая манга находится первой в инфобоксе.
* К игнорируемым в **MangaUpdates** жанрам добавлен Shoujo Ai.

### Версия 0.3.1 20250109
+ Добавлено извлечение английского наименования из **ANN**, если в **WA** оно отсутствует.
* Исправлено считывание даты из **Wikipedia** (en), когда указан только год.
* Исправлено считывание оригинального имени автора из **WA**, когда нет такого поля.
  Вместо оригинального имени копируется английское (ромадзи).

### Версия 0.3.0 20250108
+ Добавлен поиск связанной манги в **MangaUpdates**.
+ Добавлена проверка ссылок на мангу, связанную с обрабатываемой мангой на **ANN**.
+ Добавлено извлечение манги (отсутствующей на **WA**) из **ANN**.
+ Добавлено извлечение данных по манге, отсутствующих в **ANN**, из **Wikipedia** (en) и **MangaUpdates**.
+ Если дата не найдена, устанавливается заглушка `1900-01-01`.
+ Добавлено декодирование символов Юникод в наименовании.
+ Добавлено добавление формата anime.
+ Добавлен выбор пользователем наименования из **MangaUpdates**, отсутствующего в **ANN**.
+ Добавлен выбор пользователем жанров из БД для манги из **MangaUpdates**, если жанры не указаны.
+ Добавлена проверка и коррекция (по **WA**) периодичности в наименовании издания.
+ Добавлено указание типа издания при добавлении манги.
* Исправлено извлечение количества томов манги на **Wikipedia**.
* Исправлено добавление комментария об отсутствии информации о количестве томов, глав и точной даты премьеры.
* Оптимизация кода.

### Версия 0.2.0 20241205
+ Поиск количества глав манги на **Wikipedia** (en).
